# TEMPORARY — proves anonymous GHCR pull on a real runner; deleted before merge.
name: "tmp: GHCR canary"

on:
  pull_request:

jobs:
  canary:
    name: "Anonymous GHCR pull canary"
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/open-constructs/jsii-terraform@sha256:8a260318e59d1debad735c328581261f2c469584191ec50783dad135837b4127
    steps:
      - name: Verify image runs
        run: |
          terraform version
          terraform1.15.8 version
